const list_presets = pnpnd?.asset_url + "/presets/file-list/list.png";

const grid_presets = pnpnd?.asset_url + "/presets/file-list/grid.png";

const compact_presets = pnpnd?.asset_url + "/presets/file-list/compact.png";

const table_presets = pnpnd?.asset_url + "/presets/file-list/table.png";

const gallery_presets = pnpnd?.asset_url + "/presets/file-list/gallery.png";

const timeline_presets = pnpnd?.asset_url + "/presets/file-list/timeline.png";

export {
    list_presets,
    grid_presets,
    compact_presets,
    table_presets,
    gallery_presets,
    timeline_presets,
};
